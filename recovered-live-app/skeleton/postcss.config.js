// Empty on purpose: the skeleton uses plain CSS (src/styles.css), not Tailwind.
// Declaring this locally stops Vite from inheriting a parent postcss/tailwind config.
export default { plugins: {} };
